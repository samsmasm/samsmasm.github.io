# This repo deploys publicly to unisam.nz. Anything tracked here is public.
#
# Purely local ignore rules (personal notes, audit files) belong in .git/info/exclude,
# which is never committed, rather than in this file.

# Built copies of class planning docs. Source is ibecon-src/public/2025happenings/,
# which Astro copies into ibecon/ at build. See ibecon-src/.gitignore.
ibecon/2025happenings/

# npm/astro update-check artifact written into the ibecon build output
ibecon/settings.json
